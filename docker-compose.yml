version: '3'
services:
    dino-db:
        image: 'docker.io/library/postgres:14'
        environment:
            POSTGRES_PASSWORD: 'password'
            POSTGRES_DB: 'postgres'
        ports:
            - '5432:5432'
